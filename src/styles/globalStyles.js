import { StyleSheet } from "react-native";

export const globalStyles = StyleSheet.create({
  pRight10: { paddingRight: 10 },
  mLeft10: { marginLeft: 10 },
});

export const screenStyles = StyleSheet.create({
  screen1: {},
});

//export { globalStyles, screenStyles };
